// This file is deprecated - use dateUtils.js instead
import { formatTimeForDisplay as formatTime } from '../../utils/dateUtils';

// Re-export for backward compatibility
const formatTimeForDisplay = formatTime;

export default formatTimeForDisplay;